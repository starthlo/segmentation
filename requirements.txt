# Core dependencies
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0
requests>=2.31.0

# Background removal
rembg>=2.0.50

# PyTorch and SAM (Segment Anything Model)
torch>=2.0.0
torchvision>=0.15.0
segment-anything>=1.0

# GUI
# tkinter is included with Python, no need to install

# Build tool
pyinstaller>=6.0.0

# Optional: For CUDA support (if using GPU)
# torch==2.0.0+cu118  # Uncomment and adjust CUDA version as needed
# torchvision==0.15.0+cu118  # Uncomment and adjust CUDA version as needed
